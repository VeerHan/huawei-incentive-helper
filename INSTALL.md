# 华为激励数据助手 · 安装说明

## 安装

1. 将收到的 ZIP 完整解压，不要直接在压缩包内打开文件。
2. 在 Chrome 地址栏打开 `chrome://extensions`。
3. 打开右上角“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 选择解压后的 `华为激励数据助手` 文件夹；所选文件夹内应直接看到 `manifest.json`。
6. 登录华为开发者联盟，打开 2026 激励计划应用查询页并刷新。

## 更新

收到新版后，用新版文件夹替换旧文件夹，再到 `chrome://extensions` 点击插件卡片上的“重新加载”。

## 数据说明

- 每日趋势缓存在本机 Chrome 的华为开发者联盟网站数据中。
- 关闭或重启 Chrome 不会丢失；清除该网站数据后会清空。
- 插件不请求额外权限，不读取 Cookie，也不会向外部服务发送数据。

## 注意

这是开发者模式安装包，并非 Chrome 应用商店版本。Chrome 重启后如果提示开发者模式扩展，保留启用即可。
